import {iApi} from "@types";

export declare namespace iActions {
    type _getUserInfoSuccess = iApi.User.iUserInfo;
    type _getUserInfoError = iApi.User.iUserInfo;
    type setFetching = boolean;
    type _getUsersAllSuccess = iApi.Users.Item[];
}
